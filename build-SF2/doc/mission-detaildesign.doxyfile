#---------------------------------------------------------------------------
# Doxygen options to generate the complete mission documentation
#---------------------------------------------------------------------------

# Start with the common definitions, some of which are extended or overridden here.
@INCLUDE               = /opt/cfs/build-SF2/doc/cfe-common.doxyfile
PROJECT_NAME           = "SampleMission"
PROJECT_BRIEF          = "Core Flight System project built using \"SF2\" configuration"
OUTPUT_DIRECTORY       = detaildesign

INPUT                 += /opt/cfs/SF2_defs  \
                         /opt/cfs/osal/src/os/inc \
                         /opt/cfs/osal/src/os/shared \
                         /opt/cfs/psp/fsw/inc \
                         /opt/cfs/psp/fsw/shared \
                         /opt/cfs/cfe/fsw/cfe-core/src
                         
STRIP_FROM_PATH       += /opt/cfs/SF2_defs

#---------------------------------------------------------------------------
# The user content doxyfile contains paths to the mission-specific applications   
#---------------------------------------------------------------------------
@INCLUDE               = /opt/cfs/build-SF2/doc/mission-content.doxyfile
